import { Widget } from "../utils/components";

const WidgetAutoMailing = () => {
    return (
        <Widget title="Auto Mailing" index={1}>
            {/* Content for the Auto Mailing can be added here */}
        </Widget>
    );
};

export default WidgetAutoMailing;