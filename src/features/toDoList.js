import { Widget } from "../utils/components";

const WidgetToDoList = () => {
    return (
        <Widget title="To Do List" index={4}>
            {/* Content for the To Do List can be added here */}
        </Widget>
    );
};

export default WidgetToDoList;