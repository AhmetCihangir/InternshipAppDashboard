import { Widget } from "../utils/components";

const WidgetPieChart = () => {
    return (
        <Widget title="Pie Chart" index={2}>
            {/* Content for the Pie Chart can be added here */}
        </Widget>
    );
};

export default WidgetPieChart;